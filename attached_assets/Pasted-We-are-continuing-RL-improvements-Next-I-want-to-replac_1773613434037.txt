We are continuing RL improvements.
Next, I want to replace the linear function approximator with a PyTorch DQN while keeping:

    Double‑Q

    target network

    soft targets

    prioritized replay

    feature engineering

Please implement the following changes cleanly and consistently.
1. Add PyTorch and define a QNetwork

In project/agents/rl_agent.py (top of file), add:
python

import torch
import torch.nn as nn
import torch.optim as optim

Then define a small MLP before ReinforcementLearningTrader:
python

class QNetwork(nn.Module):
    def __init__(self, input_dim, output_dim):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, 64),
            nn.ReLU(),
            nn.Linear(64, 64),
            nn.ReLU(),
            nn.Linear(64, output_dim),
        )

    def forward(self, x):
        return self.net(x)

2. Replace weights with neural networks in init

In ReinforcementLearningTrader.__init__:

    Keep self.feature_dim = 8 (or whatever you already use).

    Remove self.weights = { ... } and any linear‑weight setup.

    Add:

python

self.device = torch.device("cpu")
self.action_to_index = {a: i for i, a in enumerate(self.actions)}
self.index_to_action = {i: a for a, i in self.action_to_index.items()}

self.q_net = QNetwork(self.feature_dim, len(self.actions)).to(self.device)
self.target_q_net = QNetwork(self.feature_dim, len(self.actions)).to(self.device)
self.target_q_net.load_state_dict(self.q_net.state_dict())
self.target_q_net.eval()

self.optimizer = optim.Adam(self.q_net.parameters(), lr=1e-3)
self.loss_fn = nn.MSELoss()

Keep self.target_update_tau and self.target_weights logic removed or unused; we’ll now soft‑update the target network parameters instead of manual weight lists.
3. Rewrite q_value and target_q_value

Replace the old linear versions with:
python

def q_value(self, state_vec, action):
    idx = self.action_to_index[action]
    x = torch.tensor(state_vec, dtype=torch.float32, device=self.device).unsqueeze(0)
    with torch.no_grad():
        q_vals = self.q_net(x)
    return q_vals[0, idx].item()

def target_q_value(self, state_vec, action):
    idx = self.action_to_index[action]
    x = torch.tensor(state_vec, dtype=torch.float32, device=self.device).unsqueeze(0)
    with torch.no_grad():
        q_vals = self.target_q_net(x)
    return q_vals[0, idx].item()

decide() should continue to use q_value exactly as before.
4. Rewrite update_q to train the neural network

In update_q(prev_state, action, reward, new_state):

    Convert states to vectors as before:

python

s_vec = self.state_to_vector(prev_state)
s2_vec = self.state_to_vector(new_state)

    Build tensors:

python

x = torch.tensor(s_vec, dtype=torch.float32, device=self.device).unsqueeze(0)
x2 = torch.tensor(s2_vec, dtype=torch.float32, device=self.device).unsqueeze(0)
a_idx = torch.tensor([self.action_to_index[action]], device=self.device)
r = torch.tensor([reward], dtype=torch.float32, device=self.device)

    Compute current Q(s, a):

python

q_vals = self.q_net(x)
current_q = q_vals.gather(1, a_idx.unsqueeze(1)).squeeze(1)

    Double‑Q target:

python

with torch.no_grad():
    next_q_live = self.q_net(x2)
    best_next_idx = next_q_live.argmax(dim=1)
    next_q_target = self.target_q_net(x2)
    max_next_q = next_q_target.gather(1, best_next_idx.unsqueeze(1)).squeeze(1)

    Soft target (Polyak Q‑target):

python

td_target = r + self.gamma * max_next_q
soft_target = (1 - self.tau) * current_q + self.tau * td_target
td_error = (soft_target - current_q).detach().item()

    Loss and backprop:

python

loss = self.loss_fn(current_q, soft_target)
self.optimizer.zero_grad()
loss.backward()
self.optimizer.step()

    Soft‑update target network parameters (Polyak):

python

with torch.no_grad():
    for param, target_param in zip(self.q_net.parameters(), self.target_q_net.parameters()):
        target_param.data.mul_(1 - self.target_update_tau)
        target_param.data.add_(self.target_update_tau * param.data)

    Return td_error at the end so PER can still update priorities.

python

return td_error

Note: You can keep update_eligibilities() and the eligibility dict in place, but for now the neural update is applied only to the current (s, a) pair. (Full TD(λ) with backprop is more complex; this keeps the code simple and stable.)
5. Keep replay(), PER, Double‑Q, and epsilon logic unchanged

    replay() should still:

        sample indices via prioritized replay

        unpack transitions

        call update_eligibilities() (even if traces are effectively a no‑op now)

        call update_q() and use its returned td_error to refresh priorities

    Epsilon‑greedy in decide() remains unchanged.

6. Keep behavior stable

After implementing, run project/main.py and confirm:

    All 10 episodes run cleanly

    No exploding losses or NaNs

    Replay buffer and PER still work

    Double‑Q still works

    Target network updates smoothly

    Epsilon decay still works

Please keep the code clean and consistent with the existing style.